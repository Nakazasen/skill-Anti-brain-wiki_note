# ABW v0.3.13 Stable Baseline

Release: `v0.3.13`
Commit: `d1655dc772fbe5cb82955603d125d9e5301bdfd7`
Date: `2026-04-28`

## What Is Proven

- Package/runtime version is `0.3.13`.
- Source identity survives a rename followed by later content edits, keeping later ingest output attached to the existing canonical draft instead of creating sibling drafts.
- Directory ingest can process the MOM raw tree while leaving trusted wiki promotion under explicit review control.
- Skipped-unchanged handling works for files already seen by ingest.
- Local wiki retrieval works for targeted questions.
- Broad summary requests return bounded partial answers over selected trusted wiki chunks instead of claiming full-corpus reasoning.

## Verified Commands

These commands were validated against `D:\Sandbox\MOM_WMS_QLLSSX`:

- `/abw-version`
- `/abw-health`
- `ingest raw`
- `What documents describe AGV communication?`
- `Summarize MOM WMS workflow constraints`

## MOM Validation Result

The target validation workspace was `D:\Sandbox\MOM_WMS_QLLSSX`.

- Latest observed raw tree: 58 raw files, with 53 supported sources.
- Latest ingest run: 2 files reprocessed, 51 files skipped, and 0 new conflicts.
- Workspace retained 91 wiki files, 58 draft files, and 95 historical conflict reports.
- Validation result: v0.3.13 is stable for the observed MOM workflow, including directory ingest, local wiki query, bounded broad summary, and source identity preservation.
- Scope limit: this is a single-workspace validation, not a benchmark across multiple independent workspaces.

## Known Limitations

- OCR quality depends on installed OCR language packs and local machine setup.
- Broad summaries are bounded partial retrieval over selected wiki chunks, not full corpus reasoning.
- Conflict counts include old historical conflict files.
- Orphan drafts are retained for auditability instead of being removed automatically.
- v0.3.13 has not yet been benchmarked on multiple independent workspaces.
