{
  "scope": "One governed continuation step",
  "artifact": {
    "id": "step-safe-test",
    "path": "tests/test_parser_resume.py",
    "type": "code"
  },
  "rubric": [
    {
      "id": "scope",
      "description": "Changed files stay inside the approved candidate_files.",
      "required": true,
      "passed": true
    },
    {
      "id": "validation",
      "description": "Relevant validation check passed.",
      "required": true,
      "passed": true
    }
  ],
  "checks": [
    {
      "type": "file_exists",
      "subject": "tests/test_parser_resume.py",
      "result": "pass",
      "expected": "pass",
      "required": true,
      "description": "Artifact file exists."
    },
    {
      "type": "test_result",
      "subject": "step-safe-test",
      "result": "pass",
      "expected": "pass",
      "required": true,
      "description": "Step validation result passed."
    }
  ]
}
